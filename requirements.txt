# Core
Django==5.2.15
django-simple-history==3.11.0

# Security: credential encryption at rest + login brute-force protection
cryptography==48.0.0

# Two-factor authentication (TOTP + QR provisioning)
pyotp==2.9.0
qrcode==8.2
django-axes==8.3.1
certifi==2026.5.20

# Statement parsing (CSV / XLSX / legacy XLS)
openpyxl==3.1.5
xlrd==2.0.2
python-dateutil==2.9.0.post0

# Database drivers (production). Install the one matching your DB:
#   PostgreSQL:
# psycopg[binary]==3.2.3
#   MySQL / MariaDB (recommended on cPanel/WHM):
#   - mysqlclient is faster but needs a compiler + python3-devel + mariadb-devel
# mysqlclient==2.2.4
#   - PyMySQL is pure-Python (no compiler needed) — the easy choice on cPanel:
# PyMySQL==1.1.1

# Static file serving (production; no separate web server needed)
whitenoise==6.8.2

# Production WSGI server
gunicorn==23.0.0

# Dev/test only
coverage==7.14.1

# Supporting-documents PDF (Expense Register "Supporting documents" export).
# Pure-Python wheels; safe to install on cPanel/AlmaLinux without build tools.
reportlab==4.2.5
pypdf==5.1.0
